# dbt_learn:
#   target: dev
#   outputs:
#     dev:
#       type: postgres
#       host: "{{ env_var('DBT_POSTGRES_HOST', 'host.docker.internal') }}"
#       port: 5432
#       user: "{{ env_var('DBT_POSTGRES_USER', 'postgres') }}"
#       password: "{{ env_var('DBT_POSTGRES_PASSWORD', 'postgres123') }}"
#       dbname: "{{ env_var('DBT_POSTGRES_DB', 'dbt_banking') }}"
#       schema: "{{ env_var('DBT_POSTGRES_SCHEMA', 'public') }}"
#       threads: 4


dbt_learn:
  target: dev

  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      user: postgres
      password: postgres123
      dbname: dbt_banking
      schema: public
      threads: 4